const initialState={
    number:1
}

const rootReducer = (state = initialState, action) => {
    switch (action.type){
        case 'BOUNDS':
            
            break;

    default:
        return state
    }
}

export default rootReducer